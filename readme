Install:

git clone --bare https://gitlab.com/lae/dotfiles.git "${HOME}/.cfg"
alias config='git --git-dir="${HOME}/.cfg" --work-tree="${HOME}"'
config checkout
config submodule init
config submodule update
