#!/usr/bin/env bash

set -o errexit
set -o nounset

for bat in $(ls -d /sys/class/power_supply/BAT*); do
    echo \
        $([[ $(cat "$bat/status") == "Charging" ]] && echo -n "CHR: " || echo -n "BAT: ") \
        $(( $(cat "$bat/charge_now") * 100 / $(cat "$bat/charge_full_design") ))%
done
